## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
#spring.datasource.url = ${demos.datasource.url}
#spring.datasource.username = ${demos.datasource.user}
#spring.datasource.password = ${demos.datasource.password}

spring.datasource.url = jdbc:postgresql://localhost:5432/demos
spring.datasource.username = demos
spring.datasource.password = d3m0z


# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = validate
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation = true
